{{- if not .Values.redis.cache.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mastodon.secrets.redisCacheName" . }}
  labels:
    {{- include "mastodon.labels" . | trim | nindent 4 }}
    {{- include "mastodon.userLabels" . | trim | nindent 4 }}
type: kubernetes.io/basic-auth
data:
  password: {{ required "Redis cache password is required with no existing secret" .Values.redis.cache.password | b64enc }}
{{- end }}
